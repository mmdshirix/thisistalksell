fund=false
audit=false
progress=false
legacy-peer-deps=true
# Do NOT set optional=false here, because Docker copies only package.json initially.
# We set optional=false via preinstall so it's applied before dependency install.
