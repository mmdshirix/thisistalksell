# Keep installs stable in containers and avoid native optional deps (pg-native/libpq)
optional=false
fund=false
audit=false
progress=false
# Useful for older peer-deps trees some packages still ship
legacy-peer-deps=true
