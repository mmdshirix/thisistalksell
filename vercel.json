{
  // let Vercel call pnpm with --no-frozen-lockfile so the lock can regenerate
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "next build",
  "outputDirectory": ".next"
}
